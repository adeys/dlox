// Library string

native class String {
    construct(str) {
        this.str = str;
    }

    native length;

    native toLower();

    native toUpper();

    native trim();

    native substr(start, end);

    native startsWith(str);

    native toString();
}