native class Stdin {}

native class Stdout {}

class Repl {
    static prompt(line, handler) {
        while(true) {
            Stdout.write(line);
            handler(Stdin.readline());
        }
    }
}