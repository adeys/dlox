// Library lox:math

import "lox:assert";

native class Math {
    native static pow(a, b);

    native static sin(a);

    native static cos(a);

    native static tan(a);

    native static sqrt(a);

    native static exp(a);
    
    native static log(a);

    native static pi;
}