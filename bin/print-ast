#! /usr/bin/dart

import '../src/tokens.dart';
import '../src/expr.dart';
import '../tool/AstPrinter.dart';

main() {
    if (argv.length != 1) {
		print("Usage: print-ast <file>");
		exit(1);
	} else {
        AstPrinter printer = new AstPrinter();
        printer.print()
    }	
}